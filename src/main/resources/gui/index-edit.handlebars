<div class="edit-instance-data">
    <h5>Device: <b>{{instanceName}}</b></h5>
    <div class="input-field">
        <input id="instance-name" type="text" placeholder="" value="{{instanceName}}">
        <label for="instance-name">Instance name</label>
    </div>

    <div class="fake-label">
        Instance icon
    </div>
    <img src="{{#if icon.type}}data:{{icon.type}};base64,{{icon.data}}{{else}}default-icon.png{{/if}}" alt="Device" class="circle image-icon left" data-type="{{#if icon.type}}{{icon.type}}{{/if}}" data-icon="{{#if icon.data}}{{icon.data}}{{/if}}">
    <div class="remove-icon-container" style="display: none;">
        <button type="button" class="btn filled" id="remove-icon-btn">
            <span>Remove icon</span>
        </button>
    </div>
    <div class="file-field input-field">
        <div class="btn filled">
            <span>Change icon</span>
            <input type="file" id="instance-icon" accept="image/png, image/jpg">
        </div>
        <div class="file-path-wrapper">
            <input class="file-path validate" type="text">
        </div>
    </div>

    <div class="input-field" style="clear: both; margin-top: 12rem;">
        <select id="app-groups-select" multiple class="no-autoinit">
            {{#each availableAppGroups}}
                <option value="{{id}}">{{name}}</option>
            {{/each}}
        </select>
        <label for="app-groups-select">Show these app groups in 'All my devices' tab</label>
    </div>
</div>