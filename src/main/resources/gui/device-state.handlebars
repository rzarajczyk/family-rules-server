<li id="report-{{instanceName}}">
    <div class="collapsible-header">
        <i class="material-icons">devices</i>
        <div class="instance-name"><span class="new badge total-screen-time" data-badge-caption="">Today: {{#formatScreenTime}}{{screenTimeSeconds}}{{/formatScreenTime}}</span>{{instanceName}}</div>
    </div>
    <div class="collapsible-body">
        <div class="instance-report">
        <ul class="collection">
            <li class="collection-item device-state" data-instance="{{instanceName}}">
                Device state
                <label>
                    <input name="device-state-{{instanceName}}" value="ACTIVE" type="radio" {{#ifEquals state.deviceState  "ACTIVE"}}checked{{/ifEquals}} />
                    <span>Active</span>
                </label>
                <label>
                    <input name="device-state-{{instanceName}}" value="LOCKED" type="radio" {{#ifEquals state.deviceState  "LOCKED"}}checked{{/ifEquals}} />
                    <span>Locked down</span>
                </label>
                <label>
                    <input name="device-state-{{instanceName}}" value="LOGGED_OUT" type="radio" {{#ifEquals state.deviceState  "LOGGED_OUT"}}checked{{/ifEquals}} />
                    <span>Logged out</span>
                </label>
                <span id="countdown-{{instanceName}}" style="visibility: {{#ifEquals state.deviceState "ACTIVE"}}hidden{{/ifEquals}}">
                    <hr />
                    <label>
                        <input name="countdown-{{instanceName}}" type="checkbox" {{#ifGt state.deviceStateCountdown 0}}"checked"{{/ifGt}} />
                        <span>With countdown</span>
                    </label>
                </span>
                <div class="input-wrapper">
                    <a class="btn filled right save-button">Save</a>
                </div>
            </li>
        </ul>
        </div>
    </div>
</li>