spring:
  profiles:
    active:
      - ${SPRING_PROFILES_ACTIVE:local}
  datasource:
    url: jdbc:postgresql://${PG_HOSTNAME}:${PG_PORT}/${PD_DBNAME}
    driverClassName: org.postgresql.Driver
    username: ${PG_USER}
    password: ${PG_PASS}
  exposed:
    show-sql: true

server:
  port: 8080
